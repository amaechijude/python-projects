A simple program that takes cgpa input and returns graduation grade(honours)
#Concepts Apllied
1. *try* and *except* functions
2. *if*, *elif* and *else* functions
3. User defined functions
4. User defined modules and import
